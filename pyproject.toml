[project]
name = "RhizoScan"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mne>=1.10.2",
]

[tool.pyright]
venvPath = "."
venv = ".venv"
